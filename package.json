{
  "name": "opencode-stats-engine",
  "version": "1.0.0",
  "description": "OpenCode Event-Sourced Stats Engine",
  "type": "module",
  "main": "./packages/plugin/dist/index.js",
  "types": "./packages/plugin/dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./packages/plugin/dist/index.d.ts",
      "import": "./packages/plugin/dist/index.js"
    }
  },
  "files": [
    "packages/plugin/dist",
    "packages/dashboard/dist",
    "README.md",
    "LICENSE"
  ],
  "keywords": [
    "opencode",
    "opencode-plugin",
    "stats",
    "dashboard",
    "bun"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yansongda/opencode-stats-engine"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun test --pass-with-no-tests",
    "test:watch": "bun test --watch --pass-with-no-tests",
    "biome:check": "biome check",
    "biome:fix": "biome check --fix",
    "biome:fix-unsafe": "biome check --fix --unsafe",
    "typecheck": "tsc --noEmit",
    "build": "bun run --cwd packages/plugin build",
    "build:dashboard": "bun run --cwd packages/dashboard build",
    "prepublishOnly": "bun run biome:check && bun run typecheck && bun run test && bun run build:dashboard && bun run build"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.16.0",
    "@opencode-ai/sdk": "^1.16.0",
    "hono": "^4.12.23"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@types/bun": "latest",
    "bun-types": "latest",
    "typescript": "^5.8.0"
  }
}
